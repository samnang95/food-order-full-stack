import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  static const Color primary = Color(0xFFF97316);
  static const Color secondary = Color(0xFFF59E0B);
  static const Color tertiary = Color(0xFF10B981);
  static const Color neutral = Color(0xFF1E293B);

  // Background and other common colors
  static const Color backgroundLight = Color(0xFFFFF0E6);
  static const Color backgroundDark = Color(0xFFF9FAFB);
  static const Color borderColor = Color(0xFFE5E7EB);
  static const Color subtitleColor = Color(0xFF4B5563);

  // other colors
  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black;
  static const Color transparentColor = Colors.transparent;
}