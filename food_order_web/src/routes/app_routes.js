/**
 * Application Route Paths
 */
export const AppRoutes = Object.freeze({
  ROOT: '/',
  ORDERS: '/orders',
  MENU: '/menu',
  FAVORITES: '/favorites',
  SETTINGS: '/settings',
  NOT_FOUND: '*',
});
